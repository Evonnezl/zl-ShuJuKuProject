<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.housebackend.mapper.ApplicationMapper">

    <select id="getAll" resultType="org.example.housebackend.entity.Application">
        SELECT id, user_id, house_id, type, status, department, title_level, family_size, score, request_area, original_house_id, create_time
        FROM application
    </select>

    <insert id="insert">
        INSERT INTO application(user_id, house_id, type, status, department, title_level, family_size, score, request_area, original_house_id, create_time)
        VALUES (#{userId}, #{houseId}, #{type}, #{status}, #{department}, #{titleLevel}, #{familySize}, #{score}, #{requestArea}, #{originalHouseId}, #{createTime})
    </insert>

    <update id="updateStatus">
        UPDATE application
        SET status = #{status}
        WHERE id = #{id}
    </update>

</mapper>