<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.housebackend.mapper.UserMapper">

    <select id="getAllUsers" resultType="org.example.housebackend.entity.User">
        SELECT id, name, role, phone, email
        FROM user
    </select>

    <select id="getById" resultType="org.example.housebackend.entity.User">
        SELECT id, name, role, phone, email
        FROM user
        WHERE id = #{id}
    </select>

    <select id="findByName" resultType="org.example.housebackend.entity.User">
        SELECT id, name, role, phone, email, password
        FROM user
        WHERE name = #{name}
        LIMIT 1
    </select>

    <insert id="insertUser">
        INSERT INTO user(name, role, phone, email, password)
        VALUES (#{name}, #{role}, #{phone}, #{email}, #{password})
    </insert>

    <update id="updateUser">
        UPDATE user
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="role != null">role = #{role},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="password != null">password = #{password},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>

</mapper>