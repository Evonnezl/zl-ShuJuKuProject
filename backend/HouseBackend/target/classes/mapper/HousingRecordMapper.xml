<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.housebackend.mapper.HousingRecordMapper">

    <select id="getAll" resultType="org.example.housebackend.entity.HousingRecord">
        SELECT id, user_id, house_id, score, move_in_date
        FROM housing_record
    </select>

    <insert id="insert">
        INSERT INTO housing_record(user_id, house_id, score, move_in_date)
        VALUES (#{userId}, #{houseId}, #{score}, #{moveInDate})
    </insert>

</mapper>